#frontend #react #reactnative #expo

`import { Stack } from 'expo-router'`

`Stack` - компонент, предоставляющий стековую навигацию. То-есть позволяет переходить между экранами с эффектом стека (push/pop), где каждый новый экран добавляется поверх предыдущего.


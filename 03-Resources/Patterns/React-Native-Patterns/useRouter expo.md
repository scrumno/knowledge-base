#expo #frontend  #reactnative #react 

`useRouter()` - React-hook, который возвращает объект `router` с помощью которого можно:
- переходить между страницами
	- `router.push()`
	- `router.replace()`
	- `router.back()`
	- `router.navigate()`
- читать текущий маршрут `router.pathname`
- манипулировать историей навигации, как в браузере

Этот хук - часть файловой навигации (`file-based routing`) в expo-router, которая автоматически создает маршруты на основе структуры папок в директории app/

```
app/
 ├─ index.tsx           → "/"
 ├─ profile.tsx         → "/profile"
 ├─ settings/
 │   ├─ index.tsx       → "/settings"
 │   └─ account.tsx     → "/settings/account"
 └─ (tabs)/
     ├─ home.tsx        → "/home"
     ├─ search.tsx      → "/search"
     └─ profile.tsx     → "/profile"

// пример структуры
// эти файлы автоматически становятся страницами
// useRouter() дает программный способ переходить между ними
```

## Основные методы

- `router.push({pathname, params})` - добавляет новый экран в стек навигации, то-есть можно вернуться назад с помощью `router.back()`
  можно перейти, передав параметры
🔹 Аналог в React Navigation: `navigation.navigate('Settings')`  
🔹 Аналог в браузере: `window.history.pushState()`

-  `router.replace({pathname, params})` - заменяет текущий экран без добавления нового в стек, то есть возможности вернуться назад нет
  можно перейти, передав параметры
  полезно для выхода аккаунта или успешной авторизации

- `router.back()` - возвращает на предыдущий экран (если он есть в стеке навигации).
  Аналог `navigation.goBack() or window.history.back()`

- `router.navigate(path)` - похож на .push(), но не создает дубликат экземпляра экрана, если маршрут уже активен
  часто используется между вкладками 

- `router.prefetch(path)` - загружает экран заранее, чтобы переход был мгновенным
  особенно полезно, если знаешь, что пользователь точно скоро туда пойдет (например, на следующем шаге регистрации)

## 🧩 Другие полезные свойства

| Свойство           | Описание                                                                                       |
| ------------------ | ---------------------------------------------------------------------------------------------- |
| `router.pathname`  | Возвращает путь текущего экрана (`/settings/account`)                                          |
| `router.asPath`    | Путь с параметрами, если они есть (`/user/42`)                                                 |
| `router.canGoBack` | Возвращает `true`, если можно вернуться назад                                                  |
| `router.segments`  | Массив сегментов текущего маршрута (`['settings', 'account']`)                                 |
| `router.root`      | Возвращает корневой маршрутизатор (`Root Stack`), если ты внутри вложенного `Tabs` или `Stack` |

Как понять какие элементы выносить в инверсию?

Давай составим **Чек-лист из 3 вопросов**. Если ты отвечаешь «ДА» хотя бы на один — значит, здесь нужна инверсия (Slot / Context / Render Prop).

---

### 1. Правило «Соседних Домов» (Самое важное в FSD)
В видео использовалась архитектура, похожая на FSD (Feature-Sliced Design). Там есть жесткое правило: **Фича не может знать о другой Фиче**.

**Вопрос:** *Нахожусь ли я внутри одной большой логической части (например, «Чат»), и пытаюсь ли я импортировать компонент из совсем другой большой части (например, «Платежи»)?*

*   **Пример:** Ты делаешь компонент `ChatWindow`. Тебе нужно добавить кнопку "Отправить донат".
*   **Плохо:** Внутри `ChatWindow.tsx` ты пишешь `import { DonateModal } from 'features/Donations'`. Теперь Чат сломается, если удалить модуль Донатов.
*   **Надо инвертировать:** Чат должен сказать: «Здесь место для какого-то действия под сообщением». А Страница уже вставит туда кнопку Доната.

👉 **Правило:** Если `Feature A` импортирует `Feature B` — **СРОЧНО РЕЗАТЬ**.

---

### 2. Правило «Хамелеона» (Контекст использования)
Представь свой компонент в двух разных ситуациях.

**Вопрос:** *Может ли этот компонент использоваться на другой странице, где это «вложенное действие» должно быть другим или вообще отсутствовать?*

*   **Пример:** У тебя есть `UserCard` (карточка юзера).
    *   В списке друзей на карточке должна быть кнопка **"Удалить из друзей"**.
    *   В поиске людей на той же карточке должна быть кнопка **"Добавить в друзья"**.
    *   В админке на той же карточке должна быть кнопка **"Забанить"**.
*   **Плохо:** Внутри `UserCard` писать кучу `if (isAdmin) return <BanButton /> else if (isFriend) ...`
*   **Надо инвертировать:** `UserCard` должна принимать проп (или слот) `actionSlot`.
    *   На странице друзей передаешь туда кнопку удаления.
    *   В админке передаешь кнопку бана.

👉 **Правило:** Если внутренности компонента меняются в зависимости от того, **ГДЕ** он нарисован — **ВЫНОСИМ**.

---

### 3. Правило «Тяжелого багажа»
Иногда зависимость слишком «тяжелая» или сложная.

**Вопрос:** *Мешает ли мне этот вложенный элемент тестировать или просто отображать родителя?*

*   **Пример:** У тебя есть простая форма `FeedbackForm`. Но внутри нее ты жестко зашил `MapComponent` (Google Maps), чтобы выбрать точку на карте.
*   **Проблема:**
    1. Чтобы просто показать форму в Storybook, тебе нужно грузить API Google Maps.
    2. Тесты падают без моков карты.
    3. Форма весит 5Мб из-за карты.
*   **Надо инвертировать:** Форма должна говорить: «Здесь будет какой-то Выбиратель Места».
    *   В продакшене ты передашь туда Google Maps.
    *   В тестах ты передашь простой `div` или заглушку.

👉 **Правило:** Если зависимость тянет за собой API, библиотеки или сложную логику, которая мешает жить родителю — **ВЫНОСИМ**.

---

### Когда инверсия НЕ НУЖНА? (Красный свет 🛑)

Не нужно делать дырки (слоты/контексты) для **стабильных вещей**.

1.  **UI Kit:** Не надо инвертировать кнопку. `Button` — это просто кнопка. `GameRegister` может смело импортировать `Button` из `shared/ui`. Это нормально.
2.  **Утилиты:** Функция `formatDate` или `sum(a, b)`. Они одинаковые везде.
3.  **Тесная связь:** Если `TabItem` (вкладка) не может существовать без `TabsList` (список вкладок), и они всегда используются вместе — не разрывай их.

### Итоговый алгоритм для тебя:

Смотришь на `import ... from ...` в своем файле:

1.  Это **UI Kit** (кнопка, инпут)? -> Оставляем, прямая зависимость ОК.
2.  Это **соседняя жирная фича** (Авторизация внутри Регистрации)? -> **ИНВЕРСИЯ!** (Контекст или пропсы).
3.  Этот кусок **будет меняться** на другой странице? -> **ИНВЕРСИЯ!** (render prop или children).
4.  Это **тяжелая штука** (Карта, Редактор текста), мешающая тестам? -> **ИНВЕРСИЯ!**